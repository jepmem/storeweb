<!DOCTYPE html>
<html>
<head>
  <title>Sign up</title>
  <link rel="stylesheet" href="/styles/style.css">
  <link rel="icon" type="image/png" href="/images/logo.png">
  <meta name="viewport" content="width=device-width, initial-scale=1">

</head>
<body>
  <%- include('partials/header'); %>

  <main class="auth-page">
    <h1>Sign up</h1>

    <% if (typeof error !== "undefined" && error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <form method="POST" action="/signup" class="auth-form">
      <div class="field">
        <label for="name">ชื่อที่แสดง</label>
        <input id="name" type="text" name="name" placeholder="ชื่อที่แสดง (ไม่บังคับ)">
      </div>

      <div class="field">
        <label for="email">Email</label>
        <input id="email" type="email" name="email" placeholder="you@example.com" required>
      </div>

      <div class="field">
        <label for="password">Password</label>
        <input id="password" type="password" name="password" placeholder="••••••••" required>
      </div>

      <button type="submit" class="btn btn-primary">สร้างบัญชี</button>
    </form>

    <p class="muted">มีบัญชีแล้ว? <a href="/signin">เข้าสู่ระบบ</a></p>
  </main>
</body>
</html>