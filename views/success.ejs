<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="utf-8" />
  <title>ยืนยันการซื้อเรียบร้อย</title>
  <link rel="stylesheet" href="/styles/style.css">
  <link rel="icon" type="image/png" href="/images/logo.png">
</head>
<body>
  <%- include('partials/header'); %>

  <main class="success-wrap">
    <div class="success-icon">
      <!-- checkmark (pure CSS + SVG) -->
      <svg viewBox="0 0 24 24" width="72" height="72" aria-hidden="true">
        <circle cx="12" cy="12" r="12" fill="#60d169"></circle>
        <path d="M7 12.5l3 3 7-8" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </div>
    <h1 class="success-title">ยืนยันการซื้อเรียบร้อย</h1>

    <% if (checkoutId) { %>
        <p class="success-sub">หมายเลขออเดอร์: <strong><%= checkoutId %></strong></p>
        <% } %>

    <div class="success-actions">
      <a class="btn" href="/">กลับหน้าแรก</a>
      <a class="btn outline" href="/cart">ตะกร้าสินค้า</a>
    </div>
  </main>
</body>
</html>
