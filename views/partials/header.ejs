<header class="topbar">
  <link rel="icon" type="image/png" href="/images/logo.png">
  <a class="logo" href="/">
  <img src="/images/logo.png" alt="Home" class="logo-img"></a>
  <nav class="nav">
    
    <a href="/cart" class="cart">🛒 <span class="badge"><%= cartCount %></span></a>
    <% if (user) { %>
      <a href="/profile" class="hello" style="text-decoration:none;">
    Hi, <%= user.display_name || user.email %></a>
      <form action="/logout" method="POST" style="display:inline">
        <button class="linklike" type="submit">Logout</button>
      </form>
    <% } else { %>
      <a href="/signin">Sign in</a>
      <a href="/signup">Sign up</a>
    <% } %>
  </nav>
</header>
